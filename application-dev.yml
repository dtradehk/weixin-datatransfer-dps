server:
  port: 8081
spring:
  datasource:
      name: datasource-1506
      url: jdbc:sqlserver://192.168.1.242:1433;DatabaseName=DtradeNetGarment1506
      username: sa
      password: dps0125
      # 使用druid数据源
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
      filters: stat
      maxActive: 20
      initialSize: 1
      maxWait: 60000
      minIdle: 1
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      validationQuery: select 'x'
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true
      maxOpenPreparedStatements: 20

#my weixin config
weixin:
  approve:
    mpo-url:
  import:
    mpo-url: http://localhost:8080/Weixin/ImportMpoExcessConfirm?companycode={companycode}&batchpushid={batchpushid}&uploadall={uploadall}

myprofileinfo: this is datatransfer dev profile
    